#include <iostream>

struct Node {
    int data;
    Node* next; //reference next node via pointer
}

//Setup 1st Node only
void nodeInit(struct Node *headNode, int num){
    //Node 
}

int main(){



    return 0;
}
